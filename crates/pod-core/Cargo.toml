[package]
name = "pod-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Protocol/transport core for talking to a Line6 POD X3 over USB"

[dependencies]
nusb = "0.2"
thiserror = "2"
tracing = "0.1"

[dev-dependencies]
